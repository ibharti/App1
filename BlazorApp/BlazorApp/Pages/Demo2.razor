@*Transient service*@
@page "/demo02"
@inject ContactService contactService

<h3>Singleton service Demo</h3>

@if (contactService.ContactList.Count == 0)
{
    <b>No contacts to display.</b>
}
else
{
    @foreach (var contact in contactService.ContactList)
    {
        <hr />
        <b>Name : @contact.FirstName @contact.LastName</b>
        <br />
        <i>Email : @contact.Email </i>
    }
}
